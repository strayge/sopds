services:
  sopds:
    build:
      context: .
    restart: unless-stopped
    init: true
    stop_grace_period: 30s
    ports:
      - "8000:8000"
    volumes:
      - ./config.toml:/config/config.toml:ro
      - ./library:/library:ro
      - ./data:/data
